version: '2'
services:
  mqtt:
    image: arm32v6/eclipse-mosquitto
    ports:
      - "1883:1883"
    restart: always
  pibell:
    build: ./pibell
    depends_on:
      - mqtt
    privileged: true
    restart: always
