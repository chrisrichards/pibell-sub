version: '2'
services:
  homebridge:
    build: ./homebridge
    privileged: true
    restart: always
  mqtt:
    image: panuwitp/mosquitto-arm
    ports:
      - '1883:1883'
  pibell:
    build: ./pibell
    depends_on:
      - mqtt
    privileged: true
    restart: always
