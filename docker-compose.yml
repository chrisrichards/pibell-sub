version: '2'
services:
  homebridge:
    build: ./homebridge
  mqtt:
    image: panuwitp/mosquitto-arm
    ports:
      - '1883:1883'
  pibell:
    build: ./pibell
    depends_on:
      - mqtt
    privileged: true
    restart: always
